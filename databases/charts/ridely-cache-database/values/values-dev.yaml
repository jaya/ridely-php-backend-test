architecture: standalone

global:
  redis:
    password: "redis-pass"
auth:
  enabled: true

master:
  persistence:
    enabled: true
    size: 1Gi

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 100m
      memory: 128Mi

replica:
  replicaCount: 0  # standalone

service:
  type: ClusterIP

metrics:
  enabled: true
