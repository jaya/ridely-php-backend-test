apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ridely-service.fullname" . }}-nginx-conf
data:
  nginx.conf: |-
    {{ .Files.Get "files/nginx/nginx.conf" | indent 4 }}
  app.conf: |-
    {{ .Files.Get "files/nginx/app.conf" | indent 4 }}