apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ridely-service.fullname" . }}-secret
type: Opaque
data:
  # development case
  # A senha deve estar em base64. Exemplo: senha "minha_senha" => bWluaGFfc2VuaGE=
  # TODO preciso trocar pois está com o ROOT do banco
  database-password: cmlkZWx5
  app-key: {{ .Values.app.appKey | b64enc }}