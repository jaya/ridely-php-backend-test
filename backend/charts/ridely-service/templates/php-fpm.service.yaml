apiVersion: v1
kind: Service
metadata:
  name: {{ include "ridely-service.fullname" . }}-php-fpm
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: 9000
  selector:
    app: {{ include "ridely-service.fullname" . }}-php-fpm