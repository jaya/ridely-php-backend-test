[www]
    user = www-data
    group = www-data

    listen = 9000
    pm = dynamic
    pm.max_children = 32     ; (4 core * 8 process)
    pm.start_servers = 8
    pm.min_spare_servers = 6
    pm.max_spare_servers = 12

    catch_workers_output = yes

